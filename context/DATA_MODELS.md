# Data Models (Frontend Types)

Use TypeScript interfaces:
- User, Scorecard, HoleScore
- Event, EventType, EventStatus
- UserStatistics, TrendDataPoint

API expectations (may be mocked initially):
- /api/rounds/
- /api/stats/?timeRange=month
- /api/events/ and /api/events/:id

LocalStorage keys (auth):
- clover_golf_access_token, clover_golf_refresh_token, clover_golf_token_expiry
